<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".reservation_four" />
        <activity android:name=".reservation_three" />
        <activity android:name=".reservation_two" />
        <activity android:name=".reservation_one" />
        <activity android:name=".reservation_secondactivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".ShopAcitivity" />
        <activity android:name=".ReservationAcitivity" />
        <activity android:name=".WalkActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>